{
  "scripts": {
    "start": "cd server && npm install && npm install --only=dev && npm run start:production",
    "heroku-postbuild": "cd client && npm install && npm install --only=dev && npm run build"
  }
}